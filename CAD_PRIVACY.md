# CAD 保密与发布边界

2026-09-12：用户确认 **FreeCAD 本地母版，Onshape 可选同步**。CAD 默认不公开；`.gitignore` 是防误提交措施，不是访问控制、备份或历史清理工具。

## 默认保护

- 原生母版 `.FCStd`、备份 `.FCStd1` / `.FCBak`、宏。
- STEP / BREP 等精确实体；STL / 3MF 打印网格；GLB / glTF 网页模型。网页可交互模型同样可以被下载。
- 能重建几何的 FeatureScript / OpenSCAD / Python 建模代码、精确参数表、详细几何报告。
- 后续商业化作品、未发表接口、工艺补偿与试验数据：放在 `cad/private/` 或仓库外的私有设计库，不靠文件扩展名判断。

本轮新母版和派生文件在 `cad/table-node-pair_freecad_v0.1/`，整个目录已忽略。母版可以在本地编辑，不能把“Git 忽略”当成已经有备份；应另外保存到受控的私有备份位置。

后续新节点统一放入 `cad/private/`：十字半搭接 v0.1 的母版、误差配置、建模脚本、交换件、网格、动画和详细报告都在其中。公开书稿只新增静态图与非几何状态摘要。

本次粽角开放入口、封闭入口负对照与九件小案框架均存放在 `cad/private/frame-study_v0.1/`，同样保护重建源码、BOM 尺寸、改参记录和动画。公开审阅仅复制经检查的静态图及非几何摘要，没有将整个目录打包，也没有远端上传。

后续联合误差网格、六种局部试件与实测空白模板位于 `cad/private/tolerance-study_v0.1/`。人为误差取值和完整碰撞明细只在本地；书稿只显示定性结论、案例计数及两张静态图。旧整案母版未覆盖。

## 可审阅后公开

书稿、教学示意、静态渲染及不含精确几何的进度摘要。静态图仍会披露外形；正文和尺寸标注也可能披露设计关系，因此保密作品需要逐页审阅。不能把忽略目录打包进站点部署；`.gitignore` 不会阻止程序直接复制这些文件。

若未来要公开一套打印模型，先明确该版本允许公开，再将指定派生文件放进独立发布目录，补许可证、来源身份和验证清单。不要把整个工作目录开放。

## 已存在的暴露（未自动撤销）

只读核查时，GitHub `BigJohnn/mortisetenon` 的 `visibility` 为 `public`；本地 `origin/main` 与当前提交 `b92cd49` 一致。本轮先前的 FeatureScript、参数表、建模脚本、STEP/STL/GLB 和几何报告已经被跟踪。其他既有直榫、燕尾、楔钉和间隙试件资产也在仓库中。

添加忽略规则 **不会** 从当前提交或历史中移除这些文件，也无法撤回别人已经下载的副本。本轮没有执行 `git rm --cached`、重写历史、强制推送、改仓库可见性或删除远端文件。

建议由用户确认后再分两步处理：

1. 若需要立即收紧访问，评估把仓库改为私有及对现有网站、协作者的影响；已经下载的副本仍无法收回。
2. 将受保护文件从后续提交中移除但保留本地；随后单独决定是否清理历史。历史清理需协调已有克隆和发布，不应自动执行。

## 本轮分级评估（建议，不是发布授权）

本地已跟踪风险清单共 42 项；下列分类依据 README、ASSET_CONTRACT 和当前章节链接。风险数不是泄露严重度，也没有覆盖所有可重建信息。

| 类别 | 项数 | 资产范围 | 建议 |
|---|---:|---|---|
| 已有明确教学发布记录 | 10 | Clearance Kit 的 SCAD/STL；直榫 v0.1 的 STEP、打印排版、GLB 与五件 STL | 保留既有读者入口；若继续开放，只冻结明确版本，不将新母版默认跟随公开。发布记录不等于许可证完备，后续补权利与许可审阅。 |
| 页面正在使用的 DRAFT | 10 | 燕尾 v0.2 六个 glTF、楔钉 v0.7 四个 glTF | 优先由作者决定是否继续给出可下载几何。若收紧，先替换为静态图再撤下几何，避免断链；本轮未撤下。 |
| 旧占位交换件 | 2 | `assets/downloads/dovetail-placeholder.step/.stl` | 不作为正式模型发布；建议在确认引用后退出后续版本，保留历史说明。本轮未删除。 |
| 建模工作稿与可重建源码 | 20 | 夹头/插肩旧 local-check 下 11 项、参数 JSON、FeatureScript，以及审计命中的 7 个工具脚本 | 最高优先级收紧：不再上传后续精确迭代。经用户明确确认后，停止跟踪但保留本地；历史处置另议。 |

纯扩展名扫描还会漏掉 Markdown 中的尺寸表、一般 Python 脚本、压缩包和公开 Onshape 源链接；例如 `cad/table-node-pair_v0.1.md` 仍含旧参数记录。网页不显示下载按钮也不能保护可访问的模型 URL。不能把“42 项已分类”理解成仓库已经保密。

当前落地的是防止新增暴露，不是撤销历史发布。没有改仓库可见性、Git 索引、历史或远端 Onshape 权限；没有提交、推送或建立私有备份。

## 本地预览的访问边界

`npm run dev` 默认只绑定 `127.0.0.1`；拦截 `cad/private/`、FreeCAD 双节点母版目录、所有隐藏路径（包括 `.env` / `.git`）、目录列表与指向仓库外的符号链接。对路径解码和符号链接实际目标均做检查。既有公开资产仍可读，不改变历史发布策略。

确需本地交互查看私有 CAD 时，显式使用 `npm run dev -- --private-cad`；用完停止服务器，不转发端口、不用于部署。即使此开关开启，隐藏文件与仓库外目标仍被拦截。已经运行的旧预览进程需要重启才会应用保护。

这不是面向公网的认证服务器，也不保护另一套 HTTP 服务或部署程序。部署必须从独立的经审阅发布目录打包，不能复制整个工作区。回归测试：`npm run check:preview-security`，仅使用临时合成文件。

## 提交前检查

```sh
npm run check:cad-privacy
npm run audit:cad-privacy
```

第一条检查暂存区中新加或修改的风险文件，有命中时退出码为 1；第二条列出全部已跟踪风险文件，包含过去有意发布的资产，需要人工分类。删除记录不会被拦截。

这些命令已加入项目，但**没有自动安装 Git hook 或更改 Git 配置**。运行检查不等于远端已保密。`.env` 保持忽略；当前没有发现它被 Git 跟踪。
